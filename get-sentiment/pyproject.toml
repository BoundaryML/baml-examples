[tool.poetry]
name = "app"
version = "0.1.0"
description = ""
authors = ["None"]
include = ["app"]

[tool.poetry.dependencies]
python = "^3.9"
termcolor = "^2.4.0"
baml = "^0.9.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
